spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:smev
    username: sa
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
server:
  port: 8082
